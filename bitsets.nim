# Los bitsets deben seguir la siguiente regla: high(T) < 2^16
# Son para tipos ordinales
# Tienen todas las operaciones de los conjuntos matematicos
# a in B   -> es a un elemento de B?
# a no in B -> a no es un elemento de B?
# A + B    -> A union B
# A - B    -> elimina los elementos de B en A
# A + {b}  -> agrega b al conjunto A
# A - {b}  -> quita b del conjunto A
# A * B    -> A intersección B
# A <= B   -> es A un subconjunto de B?
# A < B    -> es A un subconjunto estricto de B?
